[theme]
# Default: dark — user can switch via the sidebar toggle or ⋮ → Settings
base = "dark"
primaryColor    = "#4299e1"
backgroundColor = "#0d0f1a"
secondaryBackgroundColor = "#141726"
textColor       = "#e2e8f0"
font            = "sans serif"
